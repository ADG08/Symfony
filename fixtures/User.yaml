App\Entity\User:
  user_{1..20}:
    username: "<name()>"
    email: "<email()>"
    plainPassword: "<password()>"
    currentSubscription: '@subscription_*'
    roles: ['ROLE_USER']

  admin:
    username: "admin"
    email: "admin@admin.fr"
    plainPassword: 'admin'
    currentSubscription: '@subscription_*'
    roles: ['ROLE_ADMIN']
